[build]
  base = "rebuild-app"
  command = "npm run build"
  # `publish` is relative to `base`, so this must be ".next" (not "rebuild-app/.next").
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
